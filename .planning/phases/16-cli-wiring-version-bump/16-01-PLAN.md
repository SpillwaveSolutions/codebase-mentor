---
phase: 16-cli-wiring-version-bump
plan: 01
status: planned
depends_on: Phase 15 complete
---

# Plan 16-01: CLI Help Text Fix + Version Bump to v1.3.0

## Goal

Update CLI help text to include `gemini` in the `--for` option, bump version to 1.3.0, and verify everything works.

## Context

Phase 14 already wired `GeminiInstaller` into `_get_converters()`, so `--for gemini`, `--for all`, `status`, install, and uninstall all work. The only remaining gap is the `--for` help string, which still says `"claude, opencode, all"` — missing `gemini`. Version is currently 1.2.3 and needs to become 1.3.0.

## Tasks

### Task 1: Fix CLI help text (2 locations)
- `cli.py:26` — install command `--for` help: change to `"claude, opencode, gemini, all"`
- `cli.py:49` — uninstall command `--for` help: change to `"claude, opencode, gemini, all"`

### Task 2: Bump version to 1.3.0
- `pyproject.toml:7` — change `version = "1.2.3"` to `version = "1.3.0"`
- `ai_codebase_mentor/__init__.py:3` — change `__version__ = "1.2.3"` to `__version__ = "1.3.0"`

### Task 3: Run full test suite
- `pytest` (non-slow) — must pass 114+ tests
- Verify `--help` output includes "gemini" in the `--for` option text

### Task 4: Commit and tag
- Semantic commit: `feat(16-01): add gemini to CLI help text, bump version to 1.3.0`
- Git tag: `v1.3.0`

## Verification

- [ ] `ai-codebase-mentor install --help` shows `gemini` in `--for` description
- [ ] `ai-codebase-mentor version` outputs `1.3.0`
- [ ] All non-slow tests pass
- [ ] Git tag `v1.3.0` created

## Estimated time: 5 minutes
