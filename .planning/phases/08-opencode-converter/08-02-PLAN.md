---
phase: 08-opencode-converter
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ai_codebase_mentor/cli.py
autonomous: true
requirements:
  - OPENCODE-11
  - OPENCODE-12

must_haves:
  truths:
    - "ai-codebase-mentor install --for opencode writes OpenCode-native files"
    - "ai-codebase-mentor install --for all includes OpenCode in runtime iteration"
    - "ai-codebase-mentor uninstall --for opencode removes installed directory"
    - "ai-codebase-mentor uninstall --for all includes OpenCode"
    - "ai-codebase-mentor status reports OpenCode install state alongside Claude"
  artifacts:
    - path: "ai_codebase_mentor/cli.py"
      provides: "CLI with opencode in _get_converters() and status output"
      contains: "OpenCodeInstaller"
  key_links:
    - from: "ai_codebase_mentor/cli.py"
      to: "ai_codebase_mentor/converters/opencode.py"
      via: "import in _get_converters()"
      pattern: "from ai_codebase_mentor\\.converters\\.opencode import OpenCodeInstaller"
    - from: "ai_codebase_mentor/cli.py"
      to: "_get_converters() dict"
      via: "opencode key in registry"
      pattern: '"opencode": OpenCodeInstaller'
---

<objective>
Wire OpenCodeInstaller into the CLI so `--for opencode`, `--for all`, and `status` include OpenCode alongside Claude.

Purpose: Users can install/uninstall/check status of the OpenCode converter via the same CLI they use for Claude.

Output: Updated `ai_codebase_mentor/cli.py` with OpenCode registration
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-opencode-converter/08-CONTEXT.md
@.planning/phases/08-opencode-converter/08-01-SUMMARY.md

<interfaces>
<!-- From ai_codebase_mentor/cli.py (current state): -->
```python
def _get_converters():
    """Lazy import converters to avoid import errors if optional deps missing."""
    from ai_codebase_mentor.converters.claude import ClaudeInstaller
    return {"claude": ClaudeInstaller}
```

<!-- From ai_codebase_mentor/converters/opencode.py (created by 08-01): -->
```python
class OpenCodeInstaller(RuntimeInstaller):
    def install(self, source: Path, target: str = "global") -> None: ...
    def uninstall(self, target: str = "global") -> None: ...
    def status(self) -> dict: ...
    def _resolve_dest(self, target: str) -> Path: ...
```

<!-- CLI help text currently says "Target runtime: claude, all" -->
<!-- After this plan: "Target runtime: claude, opencode, all" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenCodeInstaller to _get_converters() and update help text</name>
  <files>ai_codebase_mentor/cli.py</files>
  <read_first>
    - ai_codebase_mentor/cli.py (current CLI — see _get_converters(), install command help text, --for option help)
    - ai_codebase_mentor/converters/opencode.py (OpenCodeInstaller to import)
    - .planning/phases/08-opencode-converter/08-CONTEXT.md (locked: registered as "opencode": OpenCodeInstaller)
  </read_first>
  <action>
Edit `ai_codebase_mentor/cli.py` with two changes:

1. In `_get_converters()`, add the OpenCode import and registry entry:
   ```python
   def _get_converters():
       """Lazy import converters to avoid import errors if optional deps missing."""
       from ai_codebase_mentor.converters.claude import ClaudeInstaller
       from ai_codebase_mentor.converters.opencode import OpenCodeInstaller
       return {"claude": ClaudeInstaller, "opencode": OpenCodeInstaller}
   ```

2. Update help text for `--for` option in both `install` and `uninstall` commands:
   - Change `help="Target runtime: claude, all"` to `help="Target runtime: claude, opencode, all"` on both `@click.option("--for", ...)` decorators
  </action>
  <verify>
    <automated>python -c "from ai_codebase_mentor.cli import _get_converters; c = _get_converters(); assert 'opencode' in c; assert 'claude' in c; print('OK: both runtimes registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from ai_codebase_mentor.converters.opencode import OpenCodeInstaller" ai_codebase_mentor/cli.py` matches
    - `grep '"opencode": OpenCodeInstaller' ai_codebase_mentor/cli.py` matches
    - `grep 'claude, opencode, all' ai_codebase_mentor/cli.py` matches (updated help text)
    - `_get_converters()` returns dict with both "claude" and "opencode" keys
  </acceptance_criteria>
  <done>"opencode" key registered in _get_converters() returning OpenCodeInstaller, help text updated</done>
</task>

<task type="auto">
  <name>Task 2: Verify status command includes OpenCode output</name>
  <files>ai_codebase_mentor/cli.py</files>
  <read_first>
    - ai_codebase_mentor/cli.py (status command — already iterates _get_converters(), so adding opencode to the dict is sufficient)
  </read_first>
  <action>
The existing `status` command already iterates `_get_converters().items()` and prints each runtime's status. Since Task 1 added "opencode" to the dict, `status` will automatically include OpenCode output. No code change needed for the status command itself.

Verify by running the CLI:
1. `python -m ai_codebase_mentor.cli status` — should show both "claude: not installed" and "opencode: not installed" lines
2. Verify the output contains exactly two runtime lines (claude and opencode)

If the status output does NOT show opencode, investigate and fix.
  </action>
  <verify>
    <automated>python -m ai_codebase_mentor.cli status 2>&1 | grep -c "opencode"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m ai_codebase_mentor.cli status` output contains a line starting with "opencode:"
    - `python -m ai_codebase_mentor.cli status` output contains a line starting with "claude:"
    - Both runtimes shown in status output (2 lines matching runtime names)
  </acceptance_criteria>
  <done>Status command shows OpenCode install state alongside Claude install state</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and confirm no regressions</name>
  <files>ai_codebase_mentor/cli.py</files>
  <read_first>
    - ai_codebase_mentor/cli.py (verify final state after edits)
    - tests/test_opencode_installer.py (ensure all 13 tests still pass)
    - tests/test_claude_installer.py (ensure no regressions)
  </read_first>
  <action>
Run the complete test suite to confirm zero regressions:

1. `python -m pytest tests/test_opencode_installer.py tests/test_claude_installer.py -v` — all tests pass
2. `python -m pytest tests/ -v` — run any other tests in the tests/ directory
3. Verify CLI commands work end-to-end:
   - `python -m ai_codebase_mentor.cli --version` — prints version
   - `python -m ai_codebase_mentor.cli status` — shows claude + opencode status
   - `python -m ai_codebase_mentor.cli install --help` — shows "claude, opencode, all" in help

If any test fails, read the failure, fix in cli.py or opencode.py, re-run.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` shows 0 failures
    - All 13 opencode tests pass
    - All existing claude tests pass
    - `python -m ai_codebase_mentor.cli install --help` output contains "opencode"
    - `python -m ai_codebase_mentor.cli status` output contains "opencode"
  </acceptance_criteria>
  <done>Full test suite passes with zero regressions; CLI install/uninstall/status all support opencode runtime</done>
</task>

</tasks>

<verification>
- _get_converters() returns {"claude": ClaudeInstaller, "opencode": OpenCodeInstaller}
- `--for opencode` accepted by install and uninstall commands
- `--for all` iterates both claude and opencode
- `status` displays both runtimes
- All tests pass (opencode + claude + any others)
</verification>

<success_criteria>
- `grep '"opencode": OpenCodeInstaller' ai_codebase_mentor/cli.py` matches
- `python -m ai_codebase_mentor.cli status` shows both "claude:" and "opencode:" lines
- `python -m pytest tests/ -v` — 0 failures
- `python -m ai_codebase_mentor.cli install --help` output includes "opencode"
</success_criteria>

<output>
After completion, create `.planning/phases/08-opencode-converter/08-02-SUMMARY.md`
</output>
