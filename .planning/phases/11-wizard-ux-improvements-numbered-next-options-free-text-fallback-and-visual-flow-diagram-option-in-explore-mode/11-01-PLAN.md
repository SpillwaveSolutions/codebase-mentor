---
phase: 11-wizard-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/codebase-wizard/skills/explaining-codebase/SKILL.md
  - ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md
  - plugins/codebase-wizard/skills/exporting-conversation/SKILL.md
  - ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Every wizard response ends with numbered options (1-5), never bare bullets"
    - "Every wizard response ends with the free-text escape hatch line after numbered options"
    - "When explaining pipelines or multi-step flows, Visual Flow is offered as a numbered option"
    - "Visual Flow output uses box-drawing characters with function name, file anchor, and purpose per node"
    - "SESSION-TRANSCRIPT.md preserves numbered format in next_options output"
  artifacts:
    - path: "plugins/codebase-wizard/skills/explaining-codebase/SKILL.md"
      provides: "Numbered options, free-text fallback, Visual Flow option type in Answer Loop"
      contains: "1. **Trace back**"
    - path: "plugins/codebase-wizard/skills/exporting-conversation/SKILL.md"
      provides: "Numbered next_options in SESSION-TRANSCRIPT output"
      contains: "1. {next_options[0]}"
    - path: "ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md"
      provides: "Bundled copy — identical to plugins/ version"
      contains: "1. **Trace back**"
    - path: "ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md"
      provides: "Bundled copy — identical to plugins/ version"
      contains: "1. {next_options[0]}"
  key_links:
    - from: "explaining-codebase/SKILL.md Step 5"
      to: "exporting-conversation/SKILL.md SESSION-TRANSCRIPT template"
      via: "next_options[] format must match between live output and export"
      pattern: "1\\. .*next_options"
---

<objective>
Update the wizard's Answer Loop to use numbered follow-up options (1-5) instead of bare bullets, add a free-text escape hatch line after every option block, add a Visual Flow option type for pipeline/orchestration explanations, and sync the SESSION-TRANSCRIPT export template to preserve numbered format.

Purpose: Keyboard-driven option selection ("just type 2") and visual flow diagrams for pipeline-heavy codebases — both observed as missing in the first real wizard session.
Output: Updated SKILL.md files (explaining-codebase and exporting-conversation) in both plugins/ and ai_codebase_mentor/plugin/ directories.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/promoted/2026-03-23-number-next-options-and-add-free-text-fallback-in-answer-loop.md
@.planning/todos/promoted/2026-03-23-add-visual-flow-diagram-option-to-explore-mode.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update explaining-codebase SKILL.md — numbered options, free-text fallback, Visual Flow</name>
  <files>
    plugins/codebase-wizard/skills/explaining-codebase/SKILL.md,
    ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md
  </files>
  <read_first>
    plugins/codebase-wizard/skills/explaining-codebase/SKILL.md
  </read_first>
  <action>
Make three changes to `plugins/codebase-wizard/skills/explaining-codebase/SKILL.md`. There are THREE locations with "Next" option blocks — all three must be updated.

**Change A — Answer Loop Step 5 "Predict Next" (around line 94-104):**

Replace the current block:
```
### Step 5 — Predict Next

End every answer with 2-3 specific follow-up options:

> **Next — want to:**
> - **Trace back**: where does this token get created?
> - **Forward**: what happens when `req.user` hits the controller?
> - **Jump**: show me the DB save on signup

Options must be specific to what was just shown. Never use generic
options like "learn more" or "continue".
```

With:
```
### Step 5 — Predict Next

End every answer with 2-5 numbered follow-up options plus a free-text escape hatch:

> **Next — want to:**
> 1. **Trace back**: where does this token get created?
> 2. **Forward**: what happens when `req.user` hits the controller?
> 3. **Jump**: show me the DB save on signup
>
> *(or just tell me what you want)*

Options must be specific to what was just shown. Never use generic
options like "learn more" or "continue". User can reply with just a
number ("2") to pick an option, or type anything in free text.

**Visual Flow option:** When the current answer explains a pipeline,
orchestration, data flow, or multi-step process, always include a
**Visual Flow** option among the numbered follow-ups:

> 1. **Visual Flow** — show me the pipeline as an ASCII diagram

When the user picks Visual Flow, generate an ASCII diagram using
box-drawing characters (`│`, `▼`, `├──`, `└──`) to show execution flow.
Label each node with: function name, file anchor (`filename:line`), and
brief purpose. Show parallel vs sequential branching clearly. Example:

```
BookGenerator.generate_book()  <- book_gen.py:95
  |
  v
1. PromptProcessor.process_prompt()      <- prompt_processor.py:12
  |
  v
2. TOCGenerator.generate_initial_toc()   <- toc_generator.py:84
  |
  v
3. TOCGenerator.refine_toc()             <- toc_generator.py:344
  |
  v
4. ChapterGenerator.generate_chapters()  <- chapter_generator.py:?
  |
  |-- For each chapter (sequential):
  |    +-- For each section (parallel, 3 workers):
  |         |-- _generate_section_draft()   <- chapter_generator.py:483
  |         |-- _refine_section()
  |         +-- _check_section_recency()
  v
5. _finalize_book()                      <- book_gen.py:154
```

After showing the diagram, offer the standard numbered follow-ups again.
```

IMPORTANT: In the actual file, use real Unicode box-drawing characters (│ ▼ ├── └──), not ASCII approximations. The example above uses ASCII due to markdown escaping constraints. The executor must write the real characters.

**Change B — Explore Mode "Step 4 - Predict Next (always)" (around line 260-270):**

Replace:
```
### Step 4 - Predict Next (always)

End every answer with options. Format:

> **Next - want to:**
> - **Trace back**: where does this token get created?
> - **Forward**: what happens when `req.user` hits the controller?
> - **Jump**: show me the DB save on signup

User can reply: `"forward"`, `"back"`, `"jump to DB"`, `"more"`, or
just describe what they want in plain language.
```

With:
```
### Step 4 - Predict Next (always)

End every answer with numbered options plus free-text escape hatch. Format:

> **Next - want to:**
> 1. **Trace back**: where does this token get created?
> 2. **Forward**: what happens when `req.user` hits the controller?
> 3. **Jump**: show me the DB save on signup
>
> *(or just tell me what you want)*

User can reply with a number ("2"), a keyword (`"forward"`, `"back"`,
`"jump to DB"`, `"more"`), or just describe what they want in free text.
When explaining pipelines or multi-step flows, include a **Visual Flow**
option (see Answer Loop Step 5 for format and diagram rules).
```

**Change C — File Mode "Step 4 — Section Transitions" (around line 313-319):**

Replace:
```
### Step 4 — Section Transitions

After each section:
> **Next — want to:**
> - **Next section**: [section name]
> - **Deeper here**: ask me more about this section
> - **Jump to**: [specific section name]
```

With:
```
### Step 4 — Section Transitions

After each section:
> **Next — want to:**
> 1. **Next section**: [section name]
> 2. **Deeper here**: ask me more about this section
> 3. **Jump to**: [specific section name]
>
> *(or just tell me what you want)*
```

After making all three changes to `plugins/codebase-wizard/skills/explaining-codebase/SKILL.md`, copy the file identically to `ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md`:

```bash
cp plugins/codebase-wizard/skills/explaining-codebase/SKILL.md ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md
```
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/codebase-mentor && grep -c '1\. \*\*Trace back\*\*' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md && grep -c 'or just tell me what you want' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md && grep -c 'Visual Flow' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md && grep -c '1\. \*\*Next section\*\*' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md && diff plugins/codebase-wizard/skills/explaining-codebase/SKILL.md ai_codebase_mentor/plugin/skills/explaining-codebase/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep '1\. \*\*Trace back\*\*' returns matches in TWO locations (Answer Loop Step 5 and Explore Mode Step 4)
    - grep 'or just tell me what you want' returns matches in THREE locations (Answer Loop, Explore Mode, File Mode)
    - grep 'Visual Flow' returns at least 3 matches (option line, heading, diagram description)
    - grep '1\. \*\*Next section\*\*' returns 1 match (File Mode Step 4)
    - No bare bullet options (- **Label**:) remain in any "Next" block — all converted to numbered
    - diff between plugins/ and ai_codebase_mentor/plugin/ copies shows no differences
  </acceptance_criteria>
  <done>All three "Next" option blocks in explaining-codebase SKILL.md use numbered format (1. 2. 3.) with free-text escape hatch. Visual Flow option type documented with diagram format and trigger conditions. Both copies (plugins/ and ai_codebase_mentor/plugin/) are identical.</done>
</task>

<task type="auto">
  <name>Task 2: Update exporting-conversation SKILL.md — numbered next_options in SESSION-TRANSCRIPT</name>
  <files>
    plugins/codebase-wizard/skills/exporting-conversation/SKILL.md,
    ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md
  </files>
  <read_first>
    plugins/codebase-wizard/skills/exporting-conversation/SKILL.md
  </read_first>
  <action>
Update `plugins/codebase-wizard/skills/exporting-conversation/SKILL.md` to use numbered format in the SESSION-TRANSCRIPT template.

**Change A — SESSION-TRANSCRIPT.md template (around lines 107-111):**

Replace:
```
**Next options explored:**
- {next_options[0]}
- {next_options[1]}
- {next_options[2]}
```

With:
```
**Next options explored:**
1. {next_options[0]}
2. {next_options[1]}
3. {next_options[2]}
```

**Change B — In-memory session buffer next_options field (around line 70):**

No change needed to the JSON schema — `next_options` remains an array. The numbering is applied only at render time in the SESSION-TRANSCRIPT template.

After making the change, copy the file identically to `ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md`:

```bash
cp plugins/codebase-wizard/skills/exporting-conversation/SKILL.md ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md
```
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/codebase-mentor && grep -A3 'Next options explored' plugins/codebase-wizard/skills/exporting-conversation/SKILL.md && diff plugins/codebase-wizard/skills/exporting-conversation/SKILL.md ai_codebase_mentor/plugin/skills/exporting-conversation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep 'Next options explored' followed by lines starting with '1.', '2.', '3.' (not '- ')
    - No bare bullet next_options lines remain (no '- {next_options' pattern)
    - diff between plugins/ and ai_codebase_mentor/plugin/ copies shows no differences
  </acceptance_criteria>
  <done>SESSION-TRANSCRIPT.md template uses numbered format (1. 2. 3.) for next_options output. Both copies are identical.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full set of changes:

1. **No bare bullet "Next" blocks remain:** `grep -n '> - \*\*' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md` should return zero matches (all converted to numbered)
2. **Free-text escape hatch present:** `grep -c 'or just tell me what you want' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md` should return 3
3. **Visual Flow documented:** `grep -c 'Visual Flow' plugins/codebase-wizard/skills/explaining-codebase/SKILL.md` should return 3+
4. **Export template numbered:** `grep '1\. {next_options' plugins/codebase-wizard/skills/exporting-conversation/SKILL.md` should match
5. **Bundled copies in sync:** `diff` between plugins/ and ai_codebase_mentor/plugin/ for both skills returns empty
</verification>

<success_criteria>
- Every "Next -- want to:" block in explaining-codebase SKILL.md uses numbered options (1. 2. 3.), never bare bullets
- Every "Next" block ends with `*(or just tell me what you want)*` free-text escape hatch
- Visual Flow option type is documented with trigger conditions (pipeline/orchestration/flow) and ASCII diagram format using box-drawing characters
- SESSION-TRANSCRIPT.md template renders next_options as numbered list
- All four files (2 explaining-codebase + 2 exporting-conversation) are updated and in-sync between plugins/ and ai_codebase_mentor/plugin/
</success_criteria>

<output>
After completion, create `.planning/phases/11-wizard-ux-improvements-numbered-next-options-free-text-fallback-and-visual-flow-diagram-option-in-explore-mode/11-01-SUMMARY.md`
</output>
